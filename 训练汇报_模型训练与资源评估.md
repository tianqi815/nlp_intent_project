# NLP 意图模型训练汇报（含资源评估）

本文基于项目 `nlp_intent_project` 在本机实际训练结果整理，重点回答模型选型、微调方法、训练效果判断与资源消耗评估，便于后续设备审批。

## 一、基底模型使用情况与选型说明

### 1) 本次训练使用的基底模型

- 训练命令使用：`--model-name modernbert-base`
- 在代码中映射为：`answerdotai/ModernBERT-base`（见 `src/model.py` 的 `get_model_mapping()`）
- 训练方式：全参数微调（`USE_LORA=False`，未启用 LoRA）

### 2) 为什么选择 ModernBERT，而不是 bert-chinese 等模型

结合当前项目代码、任务形态与工程实践，选择 `ModernBERT-base` 的主要原因：


#### Why ModernBERT?（补充：技术优势口径）

`ModernBERT`（论文：[ModernBERT](https://arxiv.org/abs/2412.13663)）代表了 BERT 架构的较新演进，相比传统 BERT 系列在架构与训练配方上做了多处改进，更适合在分类/检索等编码器任务中取得更稳健的效果与效率。

##### 1. 增强型架构（Enhanced Architecture）

- **RoPE（Rotary Position Embedding）**：更好地建模位置信息
- **GeGLU 激活函数**：改进梯度流与表示能力
- **Attention Bias Removal**：更“干净”的注意力机制
- **现代 LayerNorm 设计**：训练稳定性更好

##### 2. 训练层面的改进（Training Improvements）

- **更长上下文训练**：训练序列长度最高可到 8192 token（传统 BERT 通常为 512）
- **更高质量/更新的数据**：训练语料更“新”，覆盖更广
- **更高效的分词/词表**：tokenization 更高效
- **防过拟合技巧**：内置正则化相关的改进

##### 3. 性能收益（Performance Benefits）

下面示例用于**汇报口径**，说明“更现代的编码器架构在分类任务上通常更优/更快”；具体数字需以我们在同数据同评估下的对照实验为准：

```python
# classification 任务性能对比（示例口径，非本项目实测）
model_performance = {
    "bert-base": {"accuracy": 89.2, "inference_speed": "100ms", "memory_usage": "400MB"},
    "modernbert-base": {"accuracy": 92.7, "inference_speed": "85ms", "memory_usage": "380MB"},
}
```

#### Why Not GPT-based Models?（补充：为何不采用 GPT 类模型做意图分类）

本项目目标是**低延迟、低成本、可控可解释**的意图分类路由，因此优先选择可自托管的编码器分类模型，而不是直接用 GPT API 做分类：

| 方面 | ModernBERT（本项目路线） | GPT-3.5/4（典型） |
| --- | --- | --- |
| **延迟** | 低（通常毫秒~几十毫秒级，取决于部署） | 较高（通常 200~500ms 甚至更高，依赖网络与服务端负载） |
| **成本** | 自托管推理成本低，可控 | 按调用计费，规模化成本更高 |
| **专业化** | 可针对分类任务微调，目标明确 | 通用模型，分类需靠提示词与约束 |
| **一致性** | 输出更确定、更可复现 | 输出可能有波动（提示词/采样/服务端变化） |
| **部署** | 可离线/内网部署，数据可控 | 依赖外部 API（合规与可用性受限） |
| **上下文理解** | 双向编码（bidirectional） | 自回归（left-to-right） |

## 二、如何微调、效果不好时调什么、如何判定好坏

### 1) 当前微调流程

当前项目默认是**二阶段训练**（`python -m src.train`）：

- Stage1：`in_scope` vs `general_response`
- Stage2：`project monitoring` vs `purchasing record summary`

关键超参数来自 `src/config.py`：

- `num_train_epochs=5`
- `per_device_train_batch_size=24`
- `learning_rate=2e-4`
- `lr_scheduler_type=cosine`
- `warmup_ratio=0.1`
- `weight_decay=0.05`
- 按 epoch 做评估与保存，最佳模型指标为 `f1`

### 2) 训练效果不好时优先调整哪些指标

建议按以下顺序调参（从高收益到低收益）：

- **学习率 `learning_rate`**：先尝试 `2e-4 -> 1e-4 -> 5e-5`，过大易震荡，过小收敛慢。
- **训练轮数 `num_train_epochs`**：数据量较小时可在 3~10 之间网格试验，配合验证集 F1 观察是否过拟合。
- **批大小 `batch_size`**：受内存限制较大，CPU/GPU 资源不足时先降 batch，保持可训练性。
- **类别不平衡处理**：当前数据中 `general_response` 相对少，建议增加该类样本或做重采样/损失加权。
- **是否启用 LoRA**：资源紧张可启用 LoRA 降低显存和时间成本；若追求上限性能可继续全量微调。
- **二阶段拆分策略**：如果端到端表现差但 stage1 好、stage2 差，说明细分类别边界需要补数据或单独优化 stage2。

### 3) 如何判断训练结果好坏（项目可落地口径）

建议同时看三层指标：

- **训练内验证集指标**（`trainer.evaluate()`）：accuracy/f1/precision/recall。
- **独立测试集指标**（`python -m src.evaluate`）：重点看 `end2end_f1_macro` 与 `end2end_accuracy`。
- **业务阈值**（建议用于汇报）：  
  - `end2end_f1_macro >= 0.80`：可上线灰度  
  - `0.65~0.80`：可用于内测，需要补数据  
  - `<0.65`：不建议上线，优先补数据和调参

本次实测（独立测试集）：

- `stage1_f1 = 0.8852`（较好）
- `stage2_f1 = 0.3784`（明显偏低）
- `end2end_f1_macro = 0.5903`（整体偏低）

结论：当前主要瓶颈在 **stage2 细分类能力不足**，需要优先补充 `project monitoring` 与 `purchasing record summary` 的区分样本，并做针对性调参。

## 三、资源消耗与设备配置建议（审批重点）

## 1) 本次实测环境

- OS：Windows 11 (`10.0.26100`)
- Python：`3.13.5`
- PyTorch：`2.8.0+cpu`（无 CUDA）
- CPU：13th Gen Intel(R) Core(TM) i7-13700（16 核 / 24 线程；系统显示 2.10 GHz）
- 物理内存：32.0 GB
- 显卡：Intel(R) UHD Graphics 770（系统显示显存 128 MB）
- 存储：1.38 TB（已使用约 473 GB）

### 1.1) GPU 训练对“效果”和“效率”的影响（推论口径）

在相同数据、相同模型与相同超参条件下，**CPU 与 GPU 的主要差异是训练速度，而不是指标本身**：

- **指标（F1/Accuracy）不一定直接提升**：GPU 不会“自动让模型更聪明”，同配置下训练出的指标通常接近（差异更多来自随机性与数值精度）。
- **最终可达到的最好指标很可能间接提升**：GPU 使训练与评估更快，从而可以在相同时间内完成更多轮有效试验（学习率/epoch/batch/LoRA/基座对比），更容易把端到端指标调到更优。

结合本项目现状（stage2 明显偏弱），更推荐将 GPU 作为**提升迭代效率与调参空间**的关键资源，并配合补充 stage2 的区分样本来提升最终效果。

## 2) 本次训练数据规模

- 训练集文件：`data/train_dataset.json`
  - `project monitoring: 89`
  - `purchasing record summary: 93`
  - `general_response: 45`
  - 合计：227
- HITL 合并：`data/hitl_collected.json` 共 5 条（训练时默认 merge）
- 最终训练样本：
  - Stage1 总样本 232（切分：185/47）
  - Stage2 总样本 187（切分：149/38）
- 测试集：`data/test_dataset.json` 共 83 条

## 3) 本次训练耗时与资源实测（重点）

训练命令：

`python -m src.train --model-path bench_intent_two_stage_modernbert --model-name modernbert-base`

实测结果（CPU 全参数微调、二阶段、5 epochs）：

- **总训练耗时**：`416.72 秒`（约 `6 分 57 秒`）
- **训练进程峰值内存（Working Set）**：`4376.76 MB`（约 `4.28 GB`）
- **模型产物新增磁盘占用**：`7997.34 MB`（约 `7.81 GB`）
- **HuggingFace 缓存增量**：`0 MB`（本机已缓存模型）

评估命令（独立测试集）：

`python -m src.evaluate --model-path bench_intent_two_stage_modernbert --test-data data/test_dataset.json`

## 4) 启动与训练所需的 RAM / 磁盘建议（含环境安装）

结合本次实测与工程留量，给出设备申报建议：

- **最低可启动配置（CPU 训练）**
  - RAM：**16 GB**（可跑，但并发任务少、稳定性一般）
  - 磁盘：**至少 25 GB 可用空间**

- **推荐配置（稳定迭代）**
  - RAM：**32 GB**
  - 磁盘：**至少 40~60 GB 可用空间**
  - 原因：需要覆盖环境依赖、模型缓存、多轮训练产物归档和日志。

磁盘构成（本机实测）：

- Python 依赖目录（共享环境）：
  - `C:\Users\wwang\AppData\Roaming\Python\Python313\site-packages`：`3.38 GB`
  - `D:\miniconda\Lib\site-packages`：`2.62 GB`
  - 合计约：`6.00 GB`
- HuggingFace 缓存总占用：`18.7 GB`（其中 ModernBERT 缓存约 `572.96 MB`）
- 单次二阶段训练产物：约 `7.81 GB`

> 审批建议口径：若希望保留最近 2~3 次完整训练产物并可重复试验，建议单机预留不少于 **60 GB** 的可用磁盘。

### 4.1) 目标配置（GPU）建议：RTX 3090 + 大内存服务器

若使用以下配置进行训练（示例）：

- CPU：Intel Xeon Gold 6142（16C/32T）
- RAM：125 GiB
- GPU：NVIDIA GeForce RTX 3090（1 张）

则预期收益主要体现在：

- **训练/评估显著加速**：可将“多次试验 + 调参 + 对照实验”从小时级压到分钟级，显著提升迭代效率。
- **可用更大的 batch 或更丰富的实验矩阵**：在显存允许范围内提高 batch、启用 LoRA、或尝试更大基座（如 `modernbert-large`）进行对比。

需要强调：在不补充数据/不调整 stage2 的情况下，GPU 的主要贡献是**提速与可调参空间**；端到端指标提升仍更依赖数据质量与样本覆盖。

## 四、给领导的简版结论（可直接复述）

- 本项目当前使用 `ModernBERT-base` 作为基底模型，原因是工程链路默认适配、训练策略成熟、落地风险低。
- 微调采用二阶段分类训练；效果不佳时优先调学习率、epoch、batch，并重点补强 stage2 区分样本。
- 已完成一轮实测训练：CPU 模式约 7 分钟，峰值内存约 4.3GB，单次模型产物约 7.8GB。
- 为保障后续迭代效率和稳定性，建议审批 **32GB RAM + 40~60GB 可用磁盘**（最低可运行 16GB + 25GB）。
